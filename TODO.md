* constantly send current answers and save it in the session
* store access tokens in each session renew it
* delete sessions if they are not testing and its been too long
* frontend should close if time is up
